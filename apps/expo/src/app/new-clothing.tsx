import { Text } from "react-native";

// have the form
// upload image with expo-camera and store as file object
// that's it

const Page = () => {
   return (
      <>
         <Text>Create a new clothing</Text>
      </>
   );
};

export default Page;
