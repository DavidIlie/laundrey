import React from "react";
import { Text, View } from "react-native";

const Page = () => {
   return (
      <View className="mt-1 px-2">
         <Text>Dashboard</Text>
      </View>
   );
};

export default Page;
